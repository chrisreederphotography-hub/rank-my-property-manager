specVersion: "v1alpha"
serviceId: "seo-directory"
location: "us-central1"
schema:
  source: "schema"
  datasource:
    postgresql:
      database: "seodirectory"
      cloudSql:
        instanceId: "seodirectory-db"
connectorDirs: ["connector"]
